
  # Design RescueMate App UI

  This is a code bundle for Design RescueMate App UI. The original project is available at https://www.figma.com/design/Dt42CRUL1raMGI36bk2Mtb/Design-RescueMate-App-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  